# Claude Skills

这是一个使用 Claude Skills 开发的技能库，用于我实际生活中业务场景。

## 包含的技能

| 技能名称 | 描述 |
|---------|------|
| [pdf-beautifier](./pdf-beautifier/) | 将文档转换为精美的 A4 排版 HTML，支持浏览器直接打印为 PDF |

## 安装使用

### 方式一：用户级安装（推荐）

克隆到 `~/.claude/skills/` 目录，所有项目均可使用：

```bash
git clone https://github.com/aydengen/skills.git ~/.claude/skills
```

### 方式二：项目级安装

克隆到项目的 `.claude/skills/` 目录，仅当前项目可用：

```bash
mkdir -p .claude/skills
git clone https://github.com/aydengen/skills.git .claude/skills
```

## 技能开发规范

每个技能目录应遵循以下结构：

- **SKILL.md**（必需）：技能定义文件，包含 frontmatter 和使用说明
- **scripts/**：可执行脚本（bash、Python、Node.js 等）
- **assets/**：代码模板、样板文件、二进制资源
- **references/**：静态文档、schema、示例数据

### SKILL.md 格式

```markdown
---
name: skill-name
description: 技能描述，Gemini 据此判断何时使用该技能
---

# 技能标题

详细的使用说明和指导...
```

## 📄 许可证

MIT License
